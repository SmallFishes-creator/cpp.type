#include <iostream>
using namespace std;
int n;
int record[14];
void dfs(int count)
{
    for(int i = 1;i <= n;i++)
    {
        if(record[i] == 1)
            continue;
        
    }
}
int main()
{
    cin >> n;
    dfs(1);
    return 0;
}